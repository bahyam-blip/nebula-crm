{
  "_comment": "Only indexes that a real query needs. Firestore creates single-field indexes automatically, and every telecalling query deliberately uses one where() plus client-side sorting so it needs none. Adding speculative indexes just creates deploy work and IAM requirements for nothing.",
  "indexes": [
    {
      "collectionGroup": "insights",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "userId", "order": "ASCENDING" },
        { "fieldPath": "dismissed", "order": "ASCENDING" },
        { "fieldPath": "generatedAt", "order": "DESCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
