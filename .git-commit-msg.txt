Engine v3 + engagement truth + premium UI

ENGINE
- Worker-pool sender (shared queue, 12-way default, MAIL_SEND_CONCURRENCY 1-32):
  one slow/throttled request no longer stalls the batch (chunked Promise.all
  waited on the slowest of every 8); cancel via ONE shared stop-check per
  window (MAIL_STOP_CHECK_MS), progress serialized+throttled so state writes
  never land out of order
- MailerCloud list sync now OVERLAPS the AI copywriting (big audiences no
  longer pay sync+write serially); sync outage is non-fatal for transactional
- Analytics harvest every 4h (was 20h) so provider opens/clicks land same day

ENGAGEMENT TRUTH ("are they really delivered? nobody opens")
- A click now counts as an OPEN for the same reader (image-blocked clients
  no longer hide engaged humans); openedVia marks click-sourced opens
- Campaign docs carry lastOpenAt/lastOpenEmail, lastClickAt/lastClickEmail +
  newest-first openedSample/clickedSample (24) — human evidence, not just a counter

TEMPLATES (7 -> 10)
- aurora: dark neon glow hero, glass cards — premium/invites
- promo: offer card, benefit ticks, dashed coupon chip (P.S. becomes the claim)
- letter: serif personal founder-note — highest reply feel
- Owner style commands flow: planner extracts design_notes + template_style
  from the task wording ("festive", "dark premium"...), copywriter honours
  them; aliases dark/neon/premium->aurora, offer/discount/sale->promo,
  story/personal->letter

APP UI
- AI Email: Engagement dashboard — gradient Open-rate/Click-rate KPI tiles,
  delivered->opened->clicked->not-delivered->unsub funnel bars, facts strip;
  composer gains one-tap design-command chips (teaches the command vocabulary)
- Campaign detail: engagement panel with rate tiles, funnel, LIVE "last
  opened by X - N min ago" evidence + recent-openers chips, status pill,
  gradient header accent
- Business Profile style picker now lists all 10 templates with descriptions

Tests: NEW scripts/test_engine_v2.mjs 103/103 (10 templates tracked-render,
aliases, click-implies-open incl. dedupe + evidence, pool no-double-send,
cancel/resume, straggler-beating timing, list-outage resilience, routes);
updated stale constants in business/scale suites. 397 total green.
